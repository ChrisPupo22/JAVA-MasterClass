public class BarkingDog {
    public static boolean shouldWakeUp(boolean isBarking, int hourOfTheDay) {
        if(isBarking = true && hourOfTheDay > 0 && hourOfTheDay < 8 || true && hourOfTheDay > 22 ) {
            return true;
        } else if {

        }
    }
}

